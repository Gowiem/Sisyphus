<div class="col-md-12">
  <div class="row">
    <ul class="nav nav-pills">
      {{#each course in courses}}
        {{render courseNav course}}
      {{/each}} 
    </ul>
  </div>
  <div class="row">
    {{render 'course' selectedCourse}}
  </div>
</div>
