<div class="col-md-12">
  <div class="row">
    <ul class="nav nav-pills nav-justified">
      {{#each course in courses}}
        <li>{{render courseNav course}}</li>
      {{/each}} 
    </ul>
  </div>
  <div class="row">
    {{render 'course' selectedCourse}}
  </div>
</div>
