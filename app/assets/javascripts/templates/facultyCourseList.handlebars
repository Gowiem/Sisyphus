<ul class="nav nav-pills nav-justified">
  {{#each course in controller}}
   {{#if sameAsSelectedCourse}}
      <li class="active">{{course.title}}</li>
    {{else}}
      <li>{{course.title}}</li>
    {{/if}}
  {{/each}} 
</ul>